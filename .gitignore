# Rust build artifacts
/target

# Cargo.lock is kept (the examples are binaries, and it pins the wasm-bindgen
# version the CLI must match), so DO NOT ignore Cargo.lock.

# Editor / OS noise
*.swp
*.swo
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
